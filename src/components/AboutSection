import AnimatedSection from "./AnimatedSection";

const AboutSection = () => {
  return (
    <section className="section-dark-alt py-20 sm:py-28">
      <div className="max-w-4xl mx-auto px-4 sm:px-6">
        <AnimatedSection>
          <h2 className="font-display text-3xl sm:text-4xl font-bold text-center mb-6">
            Formação que irá <span className="text-gradient-gold">transformar</span> a sua comunicação
          </h2>
        </AnimatedSection>

        <AnimatedSection delay={0.1}>
          <p className="text-muted-foreground text-center text-lg mb-10 max-w-3xl mx-auto leading-relaxed">
            No Maestria Speakers, você vai aprender um método direto, prático e objetivo para dominar a arte de falar em público e se posicionar como autoridade.
          </p>
        </AnimatedSection>

        <AnimatedSection delay={0.2}>
          <div className="grid md:grid-cols-3 gap-6 mb-12">
            <div className="text-center p-6 rounded-lg border border-border bg-card">
              <p className="text-primary font-display text-xl font-semibold mb-2">Não é um curso comum</p>
              <p className="text-muted-foreground text-sm">É uma FORMAÇÃO COMPLETA com método próprio e prática real.</p>
            </div>
            <div className="text-center p-6 rounded-lg border border-border bg-card">
              <p className="text-primary font-display text-xl font-semibold mb-2">Você será testado</p>
              <p className="text-muted-foreground text-sm">Aqui você será exposto e transformado(a) com vivência intensa.</p>
            </div>
            <div className="text-center p-6 rounded-lg border border-border bg-card">
              <p className="text-primary font-display text-xl font-semibold mb-2">Líderes, não apenas oradores</p>
              <p className="text-muted-foreground text-sm">Estamos despertando líderes que carregam mensagens poderosas.</p>
            </div>
          </div>
        </AnimatedSection>

        <AnimatedSection delay={0.3}>
          <div className="bg-card border border-border rounded-lg p-8">
            <h3 className="font-display text-xl font-semibold text-primary mb-4">❌ Você NÃO precisa:</h3>
            <ul className="space-y-3 text-muted-foreground">
              <li className="flex items-start gap-3">
                <span className="text-primary mt-0.5">✕</span>
                Gastar anos estudando teoria de comunicação
              </li>
              <li className="flex items-start gap-3">
                <span className="text-primary mt-0.5">✕</span>
                Ser extrovertido ou "ter nascido com o dom"
              </li>
              <li className="flex items-start gap-3">
                <span className="text-primary mt-0.5">✕</span>
                Construir uma audiência gigante nas redes sociais
              </li>
              <li className="flex items-start gap-3">
                <span className="text-primary mt-0.5">✕</span>
                Ter experiência prévia em palestras
              </li>
            </ul>
          </div>
        </AnimatedSection>
      </div>
    </section>
  );
};

export default AboutSection;
